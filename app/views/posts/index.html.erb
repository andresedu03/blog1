<table class="table">
    <thead>
        <tr>
            <th>Tìtulo</th>
            <th>Fecha</th>
            <th>Imagen</th>
            <th>Contenido</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <% @post.each do |post| %>
            <td><%=post.title%></td>
            <td><%=post.created_at%></td>
            <td><%=post.image_url%></td>
            <td><%=post.content%></td>
        </tr>
        <% end %>
    </tbody>
</table>

<%= link_to 'crea un post', posts_dashboard_path %>